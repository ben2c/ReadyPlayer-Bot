# ReadyPlayer Bot

## Setup

Get discord token and place into `.env` file in the root directory.

Start Server

```bash
docker-compose up --detach
```

Refresh Server

```bash
docker-compose down
```
